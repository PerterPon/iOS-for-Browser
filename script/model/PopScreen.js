
define( function( require, exports, module ){
    "use strict";

    require( './BaseModel' );
    Ext.define( 'PopScreen', {
        extend : 'BaseModel'
    });

    return PopScreen;
});