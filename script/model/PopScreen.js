
define( function( require, exports, module ){
    "use strick";

    require( './BaseModel' );
    Ext.define( 'PopScreen', {
        extend : 'BaseModel'

    });

    return PopScreen;
});