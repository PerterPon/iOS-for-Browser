
define( function( require, exports, module ){
    //"use strict";

    require( './BaseModel' );
    Ext.define( 'BootScreen', {
        extend : 'BaseModel'
    });

    return BootScreen;
});