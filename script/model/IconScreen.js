
define( function( require, exports, module ){
    "use strick";

    require( './BaseModel' );
    Ext.define( 'IconScreen', {
        extend : 'BaseModel',

        _attachEventListener : function(){
            
        }

    });

    return IconScreen;
});