
define( function( require, exports, module ){
    "use strick";

    require( './BaseModel' );
    Ext.define( 'AssistiveScreen', {
        extend : 'BaseModel'
    });

    return AssistiveScreen;
});