
define( function( require, exports, module ){
    "use strick";

    require( './BaseView' );
    Ext.define( 'VTopBar', {
        extend : 'BaseView',
        
    });

    return VTopBar;
});