
define( function( require, exports, module ){
    //"use strict";

    require( './BaseView' );
    Ext.define( 'VPopScreen', {
        extend : 'BaseView'

    });

    return VPopScreen;
});