
define( function( require, exports, module ){
    "use strick";

    require( './BaseView' );
    Ext.define( 'VPopScreen', {
        extend : 'BaseView'

    });

    return VPopScreen;
});