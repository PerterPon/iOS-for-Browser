
define( function( require, exports, module ){
    //"use strict";

    require( './BaseView' );
    Ext.define( 'VBootScreen', {
        extend : 'BaseView'
    });

    return VBootScreen;
});