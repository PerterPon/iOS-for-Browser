
define( function( require, exports, module ){
    "use strick";

    require( './BaseView' );
    Ext.define( 'VDockIcon', {
        extend : 'BaseView',

        inheritableStatics : {
        }

    });

    return VDockIcon;
});