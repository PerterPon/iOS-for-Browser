
define( function( require, exports, module ){
    //"use strict";

    var Aminations = require( '../../animations/Anim' );
    Ext.define( 'CardView', {
        
    } );

    return CardView;
} );