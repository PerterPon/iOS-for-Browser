
define( function( require, exports, module ){
    "use strict";

    Ext.define( 'BaseModule', {

        inheritableStatics : {
        },

        statics: {
        },

        constructor: function( name ){
        }

    });

    return BaseStore;
});